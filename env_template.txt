# Kalshi High-Probability Notifier Configuration
# Copy to .env and fill in your credentials.

# =============================================================================
# KALSHI API CONFIGURATION
# =============================================================================

# Required Kalshi API credentials
KALSHI_API_KEY=your_kalshi_api_key_here

# Provide the full PEM key (include \n for line breaks) or point to a .pem file.
KALSHI_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\nyour_private_key_content_here\n-----END RSA PRIVATE KEY-----"
# KALSHI_PRIVATE_KEY_FILE=/path/to/private_key.pem

# Use the demo exchange by default
KALSHI_USE_DEMO=true

# =============================================================================
# BOT FILTERS & LIMITS
# =============================================================================

MAX_EVENTS_TO_ANALYZE=50        # Number of top events (by 24h volume) to scan
MAX_MARKETS_PER_EVENT=10        # Max markets per event (selects top N by volume)
MAX_HOURS_TO_CLOSE=24.0         # Only include markets closing within this many hours
MIN_YES_PRICE_CENTS=90          # Minimum YES bid price (in cents) to qualify
MIN_SPREAD_CENTS=1              # Minimum bid/ask spread (cents) to ensure liquidity
MIN_VOLUME_24H=1000             # Minimum 24h volume (event or market) to consider

# =============================================================================
# NOTIFICATIONS
# =============================================================================

MAX_NOTIFICATIONS_PER_RUN=5     # Send at most this many markets to Telegram each run
TELEGRAM_BOT_TOKEN=             # Bot token from @BotFather
TELEGRAM_CHAT_ID=               # Chat/channel ID (use @userinfobot to find yours)

# Keep automated trading disabled (recommended)
DISABLE_TRADING=true

# =============================================================================
# SETUP NOTES
# =============================================================================
# - Kalshi demo credentials are separate from production.
# - Start with demo mode before pointing at real markets.
# - The bot does not place orders; it only surfaces high-confidence opportunities.
